// Package bird implements the Photon routing adapter for the BIRD routing daemon
// running the Babel protocol. It is responsible for generating BIRD configuration,
// managing the BIRD child process, communicating with birdc, and observing routes.
package bird
